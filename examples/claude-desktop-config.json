{
  "_comment": "Drop this into ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) or %APPDATA%/Claude/claude_desktop_config.json (Windows). Restart Claude Desktop. Your agent gets every MCP tool whose scope your key holds. The `type: http` field is required by Anthropic's MCP JSON schema — without it some clients reject the entry.",
  "mcpServers": {
    "polyzig": {
      "type": "http",
      "url": "https://api.polyzig.com/api/mcp",
      "headers": {
        "Authorization": "Bearer pzk_live_REPLACE_ME"
      }
    }
  }
}
